# RAG 医疗问答项目第一阶段开发计划与模块接口说明

## 1. 项目范围说明

### 1.1 当前开发范围
本阶段仅开发 RAG 后端核心能力，不包含前端页面、管理后台和复杂部署优化。

本阶段包含：
- 数据读取与清洗
- 知识库构建
- 文档向量化
- 检索召回
- rerank 预留或基础接入
- LLM 回答生成
- fallback 安全降级
- 日志记录与基础评估

### 1.2 暂不包含内容
- 前端页面
- 用户登录与会话界面
- 管理后台 UI
- 可视化报表
- 生产级监控大盘
- 复杂性能优化

---

## 2. 开发原则

### 2.1 先框架后细节
先确定目录、配置、接口和数据结构，再逐模块实现。

### 2.2 模块职责单一
每个模块只处理自己的职责，不跨层写业务逻辑。

### 2.3 依赖单向流动
推荐依赖链路如下：

`data -> kb -> retrieval -> generation`

### 2.4 先跑通最小闭环
先完成“输入问题 -> 检索 -> 生成 -> 返回结果”，后续再做增强优化。

---

## 3. 推荐项目目录结构

```text
rag_medical/
├── app/
│   ├── api/                  # 对外接口层，后面再接前端
│   ├── config/               # 配置管理
│   ├── core/                 # 通用基础能力：日志、异常、统一数据模型
│   ├── data/                 # 原始数据读取、清洗、标准化
│   ├── kb/                   # 知识库构建与管理
│   ├── retrieval/            # 检索召回
│   ├── generation/           # LLM 回答生成
│   ├── evaluation/           # 评估脚本和指标
│   └── utils/                # 工具函数
├── scripts/                  # 一次性脚本、批处理脚本
├── tests/                    # 测试
├── docs/                     # 设计说明、流程说明
├── requirements.txt
└── README.md
```

### 3.1 目录职责说明

#### `app/config/`
放所有配置内容，例如：
- 模型名称
- API Key
- topk
- chunk 长度
- 路径配置
- 检索参数

#### `app/core/`
放全项目可复用的基础能力：
- 统一数据结构
- 自定义异常
- 日志初始化
- 返回格式定义

#### `app/data/`
只负责原始数据读取与清洗：
- 读取 `jsonl`
- 校验 JSON 合法性
- 展平嵌套问题结构
- 去重 question
- 处理同问题多答案
- 输出标准化数据

#### `app/kb/`
只负责知识库构建：
- 组织知识条目
- 生成可索引文档
- 附加元数据
- 准备入库数据

#### `app/retrieval/`
只负责“怎么找资料”：
- query 编码
- 向量召回
- BM25
- rerank
- 融合召回

#### `app/generation/`
只负责“怎么回答”：
- prompt 拼装
- 调用 LLM
- 安全控制
- fallback 输出

#### `app/evaluation/`
只负责评估：
- Recall@10
- Recall@5
- MRR
- 生成质量检查

#### `app/api/`
后续用于对外接口接入。

---

## 4. 模块输入输出格式

### 4.1 `data` 模块

#### 输入
原始医疗问答数据文件，例如：
- `jsonl`
- 数据集原始文件

#### 输出
标准化后的数据条目列表。

#### 建议结构
```python
{
    "id": "唯一ID",
    "question": "问题文本",
    "answer": "答案文本",
    "source": "encyclopedia",
    "split": "train",
    "meta": {
        "raw_index": 123,
        "question_type": "single"
    }
}
```

#### 说明
该模块输出应尽量简单统一，后续模块都基于这个结构工作。

---

### 4.2 `kb` 模块

#### 输入
来自 `data` 模块的标准化条目列表。

#### 输出
知识库条目列表，包含可检索文本和元数据。

#### 建议结构
```python
{
    "doc_id": "唯一ID",
    "content": "question + answer",
    "question": "问题文本",
    "answer": "答案文本",
    "metadata": {
        "source": "encyclopedia",
        "split": "train"
    }
}
```

#### 说明
`content` 是后续向量化和召回的核心文本。

---

### 4.3 `retrieval` 模块

#### 输入
- 用户问题 `query`
- 知识库索引

#### 输出
召回结果列表。

#### 建议结构
```python
{
    "query": "用户问题",
    "results": [
        {
            "doc_id": "xxx",
            "content": "召回内容",
            "score": 0.87,
            "metadata": {
                "source": "encyclopedia"
            }
        }
    ]
}
```

#### 说明
该模块的目标是为生成模块提供足够好的上下文。

---

### 4.4 `generation` 模块

#### 输入
- 用户问题 `query`
- 检索结果 `retrieval_results`

#### 输出
最终回答结果。

#### 建议结构
```python
{
    "query": "用户问题",
    "answer": "生成的回答",
    "citations": [
        {
            "doc_id": "xxx",
            "content": "引用内容"
        }
    ],
    "fallback": false
}
```

#### 说明
该模块尽量输出结构化结果，方便后面接前端和日志。

---

### 4.5 `evaluation` 模块

#### 输入
- 检索结果
- 标注答案
- 生成答案

#### 输出
评估指标结果。

#### 建议结构
```python
{
    "recall@5": 0.72,
    "recall@10": 0.81,
    "mrr": 0.68,
    "sample_count": 1000
}
```

#### 说明
该模块尽量只做统计，不掺杂业务逻辑。

---

## 5. 推荐的统一数据模型

为了避免每个模块返回格式不一致，建议定义统一模型。

### 5.1 `Document`
表示知识条目。

包含：
- `doc_id`
- `question`
- `answer`
- `content`
- `metadata`

### 5.2 `RetrievalResult`
表示召回结果。

包含：
- `doc_id`
- `content`
- `score`
- `metadata`

### 5.3 `GenerationResult`
表示回答结果。

包含：
- `query`
- `answer`
- `citations`
- `fallback`
- `latency`

### 5.4 `EvaluationResult`
表示评估结果。

包含：
- `recall@5`
- `recall@10`
- `mrr`
- `sample_count`

---

## 6. 第一阶段开发顺序

### 第 1 步 初始化项目骨架
- 创建项目目录结构
- 统一配置文件
- 统一日志规范
- 统一返回结构
- 定义数据模型

### 第 2 步 数据读取与清洗
- 读取 `jsonl` 数据
- 校验 JSON 合法性
- 展平问题的嵌套 list
- 去重 question
- 处理同问题多答案
- 过滤异常样本
- 截断或切分超长 answer

### 第 3 步 知识库构建
- 定义知识条目结构
- 生成 `question + answer` 文档内容
- 附加元数据
- 支持批量导入

### 第 4 步 文档向量化与索引
- 接入 embedding 模型
- 批量生成向量
- 存储到向量库
- 接入 Milvus BM25 Function 与 sparse 索引

### 第 5 步 检索召回模块
- query 标准化
- 支持 `hybrid`、`dense`、`sparse` 三种 TopK 召回模式
- 基础结果排序
- 返回引用信息
- 失败 fallback

### 第 6 步 RRF 融合召回
- 使用 Milvus `RRFRanker(k=60)` 融合 dense 与 BM25 候选
- 保留后续 cross-encoder 或 LLM 精排扩展位置
- 设计可扩展召回链路

### 第 7 步 LLM 回答生成
- 编写 prompt 模板
- 注入检索上下文
- 控制回答结构
- 处理无结果场景
- 输出安全提示

### 第 8 步 日志与评估
- 记录用户问题
- 记录召回结果
- 记录生成结果
- 记录耗时
- 记录异常
- 计算 Recall@10、Recall@5、MRR

---

## 7. 第一阶段验收标准

第一阶段结束时，至少要满足以下条件：
1. 能成功读取并清洗医疗问答数据
2. 能生成标准化知识条目
3. 能把知识条目向量化并建立索引
4. 能根据用户问题召回相关内容
5. 能根据召回结果生成回答
6. 无召回结果时能安全降级
7. 能记录关键过程日志
8. 能输出基础评估结果

---

## 8. 结论

本项目第一阶段建议采用“先框架、后模块、再优化”的方式推进。这样可以保证结构清晰、职责分明、低耦合，并且便于后续接入前端和继续扩展功能。

---

## 9. 当前实现进展：Milvus 原生混合召回

当前已完成原计划中的 BM25 与混合召回能力，不再是接口预留状态。

### 9.1 支持的检索模式
- `hybrid`：默认模式，同时执行 dense 与 sparse 召回，再由 Milvus `RRFRanker(k=60)` 融合
- `dense`：使用 `embedding` 字段执行 `HNSW + COSINE` 语义检索
- `sparse`：使用 Milvus BM25 Function 自动生成的 `sparse_embedding` 执行关键词检索

### 9.2 Score 说明
- dense 模式返回 COSINE 相似度
- sparse 模式返回 BM25 分数
- hybrid 模式返回 RRF 排名融合分数，公式为 `Σ 1 / (k + rank_i(document))`
- 三种分数尺度不同，不能直接横向比较
- 当前 `min_score` 默认值为 `0.0`，后续需要按模式分别通过评测集校准

### 9.3 API 参数
`POST /query` 新增 `search_method` 参数，可选值为 `hybrid`、`dense`、`sparse`，默认值为 `hybrid`。

详细 schema、召回流程与算分示例见 `docs/architecture.md`。
